<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:scrollbarAlwaysDrawVerticalTrack="true" >

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

        <ImageView
            android:id="@+id/maths1"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/maths1" >
        </ImageView>

        <ImageView
            android:id="@+id/maths2"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/maths2" >
        </ImageView>

        <ImageView
            android:id="@+id/mp1"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/mp1" >
        </ImageView>

        <ImageView
            android:id="@+id/mp2"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/mp2" >
        </ImageView>

        <ImageView
            android:id="@+id/sp1"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/sp1" >
        </ImageView>

        <ImageView
            android:id="@+id/sp2"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/sp2" >
        </ImageView>

        <ImageView
            android:id="@+id/coa1"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/coa1" >
        </ImageView>

        <ImageView
            android:id="@+id/coa2"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/coa2" >
        </ImageView>

        <ImageView
            android:id="@+id/cn1"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/cn1" >
        </ImageView>

        <ImageView
            android:id="@+id/cn2"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/cn2" >
        </ImageView>

        <ImageView
            android:id="@+id/iot"
            android:contentDescription="Specs"
            android:adjustViewBounds="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:visibility="gone"
            android:src="@drawable/iot" >
        </ImageView>
    </LinearLayout>
</ScrollView>